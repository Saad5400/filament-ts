{
  "name": "driven-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false",
    "test": "npm run build && node ./bin/test.js",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "devDependencies": {
    "@adonisjs/eslint-config": "latest",
    "@japa/runner": "^3.1.4",
    "eslint": "latest",
    "prettier": "latest",
    "typescript": "latest"
  }
}
